{
  "name": "ponto_digital",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview --host 127.0.0.1 --port 4173 --strictPort",
    "preview:build": "npm run build && vite preview --host 127.0.0.1 --port 4173 --strictPort"
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "vite": "^8.0.10"
  }
}
